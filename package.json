{
  "name": "agentdock-workspace",
  "private": true,
  "packageManager": "yarn@1.22.22",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "typecheck": "yarn build:contracts && yarn build:checkpoint && yarn typecheck:contracts && yarn typecheck:checkpoint && yarn typecheck:agentdock && yarn typecheck:checkpoint-postgres && yarn typecheck:checkpoint-sqlite && yarn typecheck:checkpoint-mongodb && yarn typecheck:checkpoint-redis && yarn typecheck:models",
    "test": "yarn workspace @agentdock-ai/contracts test && yarn workspace @agentdock-ai/checkpoint test && yarn workspace @agentdock-ai/agentdock test && yarn workspace @agentdock-ai/checkpoint-postgres test && yarn workspace @agentdock-ai/checkpoint-sqlite test && yarn workspace @agentdock-ai/checkpoint-mongodb test && yarn workspace @agentdock-ai/checkpoint-redis test && yarn workspace @agentdock-ai/models test",
    "test:coverage": "yarn workspace @agentdock-ai/agentdock test:coverage",
    "test:integration:services": "yarn build && yarn workspace @agentdock-ai/agentdock test:integration",
    "build": "yarn build:contracts && yarn build:checkpoint && yarn build:agentdock && yarn build:checkpoint-postgres && yarn build:checkpoint-sqlite && yarn build:checkpoint-mongodb && yarn build:checkpoint-redis && yarn build:models",
    "ci": "yarn format:check && yarn typecheck && yarn test && yarn test:coverage && yarn build",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release": "yarn build && changeset publish",
    "typecheck:contracts": "yarn workspace @agentdock-ai/contracts typecheck",
    "typecheck:checkpoint": "yarn workspace @agentdock-ai/checkpoint typecheck",
    "typecheck:agentdock": "yarn workspace @agentdock-ai/agentdock typecheck",
    "typecheck:checkpoint-postgres": "yarn workspace @agentdock-ai/checkpoint-postgres typecheck",
    "typecheck:checkpoint-sqlite": "yarn workspace @agentdock-ai/checkpoint-sqlite typecheck",
    "typecheck:checkpoint-mongodb": "yarn workspace @agentdock-ai/checkpoint-mongodb typecheck",
    "typecheck:checkpoint-redis": "yarn workspace @agentdock-ai/checkpoint-redis typecheck",
    "typecheck:models": "yarn workspace @agentdock-ai/models typecheck",
    "build:contracts": "yarn workspace @agentdock-ai/contracts build",
    "build:checkpoint": "yarn workspace @agentdock-ai/checkpoint build",
    "build:agentdock": "yarn workspace @agentdock-ai/agentdock build",
    "build:checkpoint-postgres": "yarn workspace @agentdock-ai/checkpoint-postgres build",
    "build:checkpoint-sqlite": "yarn workspace @agentdock-ai/checkpoint-sqlite build",
    "build:checkpoint-mongodb": "yarn workspace @agentdock-ai/checkpoint-mongodb build",
    "build:checkpoint-redis": "yarn workspace @agentdock-ai/checkpoint-redis build",
    "build:models": "yarn workspace @agentdock-ai/models build"
  },
  "devDependencies": {
    "@changesets/cli": "^2.29.7",
    "prettier": "^3.5.3"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
